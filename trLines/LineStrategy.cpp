#include "stdafx.h"
#include "LineStrategy.h"

/*
void app::LineStrategy::DrawLine( ByteImg & data, const LineSeg & line, app::Color3 color )
{
	DrawLine( data, line.p1.x, line.p1.y, line.p1.x + line.p2.x, line.p1.y + line.p2.y, color );
}


app::ColorFloat app::LineStrategy::DrawLine( FloatImg & data, const LineSeg & line, app::ColorFloat & color, std::function<float( float )> f )
{
	return DrawLine( data, line.p1.x, line.p1.y, line.p1.x + line.p2.x, line.p1.y + line.p2.y, color, f );
}
*/